$(document).ready(function(){

	/* -- out- typed js --*/
	var typed = new Typed('.typing-1', {
		strings: ["Mehedihasan shamim",
		],
		loop:true,
		backSpeed:100,
		typeSpeed: 100,
		backDelay: 900
	});

});